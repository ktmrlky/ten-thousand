export const DANGER_ALERT = (header, contentx) => ({
  variant: "danger",
  heading: header,
  content: contentx,
  show: true,
});

export const SUCCESS_ALERT = (header, contentx) => ({
  variant: "success",
  heading: header,
  content: contentx,
  show: true,
});
